import { ConvertLetterPipe } from './convert-letter.pipe';

describe('ConvertLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
